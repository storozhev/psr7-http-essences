language: php

php:
  - 7.1
  - 7.2
  - hhvm # on Trusty only
  - nightly

before_script:
  - if [[ $TRAVIS_PHP_VERSION == 'hhvm' ]]; then curl -sSfL -o ~/.phpenv/versions/hhvm/bin/phpunit https://phar.phpunit.de/phpunit-5.7.phar
; fi

install: travis_retry composer install

notifications:
  email: falsec